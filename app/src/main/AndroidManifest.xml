<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logoapp"
        android:label="Escola Conecta"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicativoTranport">

    <!-- 🟣 Tela de Splash (inicial / launcher) -->
        <activity
            android:name=".ui.slash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🟢 Tela de Login -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />

        <!-- 🟡 Tela de Cadastro -->
        <activity
            android:name=".ui.login.CadastroActivity"
            android:exported="false" />

        <!-- 🔵 Tela Principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.home.AddAvisoActivity" />
    </application>

</manifest>
